#include <iostream>

/*
This program uses the bisection method to approximate the value of R, where are is 
the resistance on a circuit such that the charge on that circuit dissipates 1% of its
initial value in 0.05 seconds. That is to say q(0.05)/q0 = 0.01. L = 5H and C = 10^-4F.
*/

int main()
{
    std::cout << "Hello World" << std::endl;
    return 0;
}